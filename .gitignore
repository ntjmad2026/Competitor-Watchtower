# Secrets — never commit. .env.* also catches backups (.env.bak, .env.practice-backup);
# only the credential-free template is allowed through.
.env
.env.*
!.env.example
apikey.txt

# Personal / volatile notes (not for version control)
CLAUDE.local.md

# Dependencies
node_modules/

# OS cruft
.DS_Store
