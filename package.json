{
  "name": "watchtower-server",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Local server for the Competitor Watchtower dashboard, backed by the Claude Agent SDK.",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "test:live": "node test.js --live"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.185"
  }
}
